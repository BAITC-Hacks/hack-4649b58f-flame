name: Temporary runner probe

on:
  push:
    branches:
      - feature/agent
    paths:
      - ".github/workflows/tmp-agent-tests.yml"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: python --version
