-r requirements.txt
-r requirements-agent.txt

# Local multilingual speech recognition on NVIDIA/Linux. No audio is sent to an API.
faster-whisper>=1.1,<2

# Optional diarization can be installed separately after gated model access is
# granted. Without it, segments retain speaker_id="UNKNOWN".
